/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // ❌ Do NOT set output:'export'
  // ❌ Do NOT set basePath (unless you intend to use /<basePath>/landing)
};
export default nextConfig;
